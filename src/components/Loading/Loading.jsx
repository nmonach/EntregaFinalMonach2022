import { useEffect } from "react"

const Loading = ()=>{
    useEffect(()=>{
        return ()=>{
            
        }
        
    }) 
return (
    <h2>Cargando...</h2>
    ) 
}

export default Loading